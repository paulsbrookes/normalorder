from setuptools import setup, find_packages, Extension

setup(name='normalorder',
      version='1.0.0',
      description='Tools for normal ordering ladder operators.',
      packages=find_packages())
